#include <iostream>
#include <fstream>
#include <vector>
#include <algorithm>

std::vector<int> readData() {
    std::ifstream fs("input", std::ios_base::in);
    std::vector<int> data;

    return data;
}

int main() {
    std::vector<int> data = readData();

    int result = -1;


    std::cout << result;
    return 0;
}
